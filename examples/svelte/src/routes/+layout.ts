// The example is a pure client-side SPA: the diagram library lives in
// localStorage, so there is nothing to render or prerender on the server.
export const ssr = false;
export const prerender = false;
